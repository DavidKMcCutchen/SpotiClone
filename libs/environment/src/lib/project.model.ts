export interface ProjectEnvironment {
  production: boolean;
  apiUrl: string;
  // client_id: string;
  // client_secret: string;
  // redirect_uri: string;
  // scopes: string;
  // spotifyUrl: string;
  // spotifyTokenUrl: string;
};